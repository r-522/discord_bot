{
  "name": "it-term-rag-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:migrate": "wrangler d1 execute IT_TERMS_DB --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.8.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.16.1"
  }
}
